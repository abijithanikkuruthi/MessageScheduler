version: '3'

services:
  messagehandler:
    build: ./MessageHandler
    image: abianik/messagehandler
    volumes: 
      - ./MessageHandler:/app
    environment:
      - KAFKA_SERVER=192.168.1.4:9092